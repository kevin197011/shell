shell::utils::import() {
    module=$1
    if [[ -d ${module} ]]; then
        for file in ${module}/*; do
            ${DEBUG} && echo "import $(basename ${file})"
            source ${file}
        done
    else
        ${DEBUG} && echo "import $(basename ${module})"
        source ${module}
    fi
}
